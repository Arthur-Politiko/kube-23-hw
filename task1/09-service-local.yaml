---
apiVersion: v1
kind: Service
metadata:
  name: task-service
  namespace: task1
spec:
  type: ClusterIP
  # clusterIP: None
  selector:
    app: web-app
  ports:
    - name: web
      port: 80
      targetPort: 80